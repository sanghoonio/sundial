# Sundial Feature Batch: API Alignment, Notes UX, Import/Export, Toast Removal

## Implementation Order

1. **Feature 7** — Remove toasts first (simplifies subsequent work, removes dead imports)
2. **Feature 1A-1E** — API alignment (backend then frontend)
3. **Feature 2** — New note autosave behavior
4. **Feature 8** — Double-click preview toggle
5. **Feature 3** — Note import
6. **Feature 4** — Note export
7. **Feature 5** — Workspace export
8. **Feature 6** — Workspace restore

## Implementation Log

_(will be appended as features are completed)_
