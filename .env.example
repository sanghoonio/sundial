# Sundial Configuration

# Secret key for JWT tokens (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=change-me-to-a-random-secret

# Database path (relative to project root)
DATABASE_URL=sqlite+aiosqlite:///./workspace/sundial.db

# Workspace directory for markdown files
WORKSPACE_DIR=./workspace

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# AI Configuration (optional - leave empty to disable)
ANTHROPIC_API_KEY=

# Calendar Sync (optional - leave empty to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
